export {}

// import React, { createContext, useState } from 'react'

// type State = { open: boolean }
// const defaultValue = { open: false }
// type SidebarProviderProps = { children: React.ReactNode }

// function toggleSidebar(state: State) {
//   return { open: !state.open }
// }

// const SidebarContext = React.createContext<State>(defaultValue)

// const SidebarProvider = ({ children }: SidebarProviderProps) => {
//   const [state, setState] = useState(defaultValue)

//   const value = state

//   return <SidebarContext.Provider> {children}</SidebarContext.Provider>
// }
