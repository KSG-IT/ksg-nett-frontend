import styled from 'styled-components'

const Wrapper = styled.div`
  display: flex;
  flex-direction: row;
`
export const ApplicantRow: React.VFC = () => {
  /** 
   * This component handles two things
   *  > Display the state of an applicant
   *  > Users voulenteering to be an interviewer
   * 

   */
  return <Wrapper></Wrapper>
}
