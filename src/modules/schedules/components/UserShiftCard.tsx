import { Card } from '@mantine/core'

type ShiftNode = {}

interface UserShiftCardProps {
  shift: ShiftNode
}
export const UserShiftCard: React.VFC<UserShiftCardProps> = ({ shift }) => {
  return <Card>Briuh</Card>
}
