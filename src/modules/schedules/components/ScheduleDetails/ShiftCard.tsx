import {
  ShiftDay,
  ShiftLocationDayGroup,
  ShiftNode,
} from 'modules/schedules/types.graphql'

interface ShiftCardProps {
  shift: ShiftNode
}
export const ShiftCard: React.FC<ShiftCardProps> = ({}) => {
  return <div>aposkd</div>
}
